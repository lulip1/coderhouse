npm run biuld-css este es para compilar

npm run watch-css este se ejecuta solo cada vez que guardamos un archivo de sass. Se ejecuta solo y cambia el css solo

GIT
git status
git add .
git commit -m"descripcion"
git log
git checkout
git log --oneline

